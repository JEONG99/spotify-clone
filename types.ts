export interface UserDetails {
  id: string;
  full_name: string;
  avatar_url: string;
}

export interface Song {
  id: string;
  title: string;
  image_path: string;
  song_path: string;
  genre: string;
}
